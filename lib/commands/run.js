/*!
 * let - Force timeouts.
 *
 * Veselin Todorov <hi@vesln.com>
 * MIT License.
 */

/**
 * Run command.
 *
 * @returns {Object} Runner.
 */
function run(cmd) {
};

/**
 * Expost `run`.
 */
module.exports = run;
